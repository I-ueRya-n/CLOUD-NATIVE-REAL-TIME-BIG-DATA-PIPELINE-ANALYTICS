\documentclass[]{article}

%opening
\usepackage{amsmath,amssymb,amsfonts,amsthm,braket,cancel}
\usepackage{listings,xcolor,enumitem,tikz,physics,pgfplots}
\usetikzlibrary{calc,decorations.markings,positioning}
\usepackage[margin=1.0in]{geometry}
\usepackage{bbold}
\newcommand*{\QED}{\null\nobreak\hfill\ensuremath{\blacksquare}}
\numberwithin{equation}{section}
\usepackage{xurl,algorithm,algpseudocode}
\usepackage{fancyhdr,pdfpages}
\usepackage{bytefield,pgf-umlsd,svg}

\setlength{\parindent}{0em}

\newcommand{\newthreadShift}[4][gray!30]{
  \newinst[#4]{#2}{#3}
  \stepcounter{threadnum}
  \node[below of=inst\theinstnum,node distance=0.8cm] (thread\thethreadnum) {};
  \tikzstyle{threadcolor\thethreadnum}=[fill=#1]
  \tikzstyle{instcolor#2}=[fill=#1]
}

\lstset{ %
	language=sh,                     % the language of the code
	basicstyle=\footnotesize,       % the size of the fonts that are used for the code
	numbers=left,                   % where to put the line-numbers
	numberstyle=\tiny\color{gray},  % the style that is used for the line-numbers
	stepnumber=1,                   % the step between two line-numbers. If it's 1, each line
	% will be numbered
	numbersep=5pt,                  % how far the line-numbers are from the code
	backgroundcolor=\color{white},  % choose the background color. You must add \usepackage{color}
	showspaces=false,               % show spaces adding particular underscores
	showstringspaces=false,         % underline spaces within strings
	showtabs=false,                 % show tabs within strings adding particular underscores
	frame=single,                   % adds a frame around the code
	rulecolor=\color{black},        % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
	tabsize=2,                      % sets default tabsize to 2 spaces
	captionpos=b,                   % sets the caption-position to bottom
	breaklines=true,                % sets automatic line breaking
	breakatwhitespace=false,        % sets if automatic breaks should only happen at whitespace
	title=\lstname,                 % show the filename of files included with \lstinputlisting;
	% also try caption instead of title
	keywordstyle=\color{blue},      % keyword style
	stringstyle=\color{teal},      % string literal style
	escapeinside={\%*}{*)},         % if you want to add a comment within your code
	morekeywords={*,...}            % if you want to add more keywords to the set
} 

%\begin{figure}[h]
%    \centering
%    \includegraphics[scale=0.35]{.png}
%    \caption{}
%\end{figure}

\renewcommand{\arraystretch}{1.5}

\title{
    \vspace{2cm}

}
\date{}
\author{}

\begin{document}

\begin{titlepage}
    \begin{center}
        \vspace*{1cm}

        \textbf{Cluster and Cloud Computing (COMP90024)}
        \vspace{0.5cm}

        \textbf{Assignment 2: Big Data Analytics on the Cloud}
        \vspace{1cm}

        \large{\textbf{Social Media and Parliamentary Data Analysis}}
        \vspace{1cm}

        Team 57 \\
        \vspace{0.2cm}
        Josh Childs (1549150) \qquad Enzo Noel (1675346) \\ 
        Molly Stow (1356089) \qquad YU-WEI LIN (1537312) \\

        \vfill 

        21 May 2025 \\
        Faculty of Engineering and IT \\ 
        The University of Melbourne \\ 
        Australia\\
    \end{center}
\end{titlepage}

\pagestyle{fancy}
\fancyhead[L]{Team 57}
\fancyhead[C]{COMP90024 - Assignment 2}
\fancyhead[R]{5 May 2025}

GitLab Repo: \url{https://gitlab.unimelb.edu.au/JCCHIL/comp90024_team_57} \newline
Video: \url{https://}

\section*{Scenarios}

\paragraph*{Overview}
Today, it is nearly impossible to spend time online without witnessing a discussion about politics. For many people, this is not a topic that is discussed politely, calmly and with respect. A thread on a hot topic quickly can become intense, escalating into arguments and attacks. 

But is it truly like this everywhere, or are we just seeing the worst side of the internet? 

Are there still sides of social media where people present their political opinions in a pleasant and happy way? 
And where does all this anger come from? 
Are Australians egged on by high valence political speeches? Do some politicians and parties use more aggressive language, and does this align with the discussions they spark? 


In this project, we set out to answer these questions by analysing political discussions held on two different platforms: Bluesky and Reddit.
We thought it would be interesting to compare the sentiment and topic of these discussions to those of political speeches being held in parliament, as provided through OpenAustralia.

\subsection*{Analysis Goals}
We had three main goals for our analysis.

\paragraph*{Comparing the topics}
We first wanted to see what political topics were popular for discussion across platforms. 
Do users of different social media platforms have different priorities? Do they discuss the same topics, and do these align with what is being discussed in parliament?
We investigated topics such as “housing”, and entities, including politicians such as “Pauline Hansen”, Australian locations, and parties, such as "Labor".

\paragraph*{Comparing the overall sentiment}
We aimed to contrast the overall sentiment of these discussions and debates over time.
We wanted to see if the conversations held on some of these platforms were more positive or negative than others, and whether there were any clear trends.

\paragraph*{Comparing the sentiment by topic}
We also wanted to see if the sentiment of these discussions differed depending on the topic being discussed.
For example, do users of Reddit post more positively about the topic of housing than those on BlueSky?

\paragraph*{Comparing the topics over time}
We then wanted to contrast the sentiment and topics of these social media discussions to the actual debates being held. 
This allowed us to see if topics were discussed immediately after a relevant debate, and whether an intense debate correlated with a shift in sentiment online.
\section*{System Architecture}
\subsection*{Overview}
The system is designed to gather data from three different platforms: Bluesky, OpenAustralia and Reddit.
The data is processed by fission functions and stored in elastic search indices.
Analysis is performed by seperate fission functions, and the result of any analysis is cached in elastic search for future requests.
User facing functions aggregate results from the analysis functions, which is displayed to the user in a Jupyter Notebook.
The system is designed to be modular and scalable, so that we can easily add new platforms or data sources in the future and handle varying amounts of data.
A high level overview of the architecture is show in Figure~\ref{fig:arch}. 

\begin{figure}[h]
    \centering
    \includegraphics[scale=0.25]{report_images/architecture.png}
    \caption{System Architecture. White rectangles are Elastic Search Indices, Grey rectangles are external data sources and colored rectangles are fission functions.}
    \label{fig:arch}
\end{figure}

\subsection*{Bluesky}

Bluesky has a firehose which gives access to all changes on the bluesky network.
To handle the data from the firehose, we split the processing into two parts.
The first part is receiving data from the firehose, which is written in Go and containerized in a docker image.
The docker image is deployed to kubernetes, with a single replica so kubernetes will restart the container if an error occurs.
The second part is decoding and extracting the useful information from a repository commit.
The AT protocol sends data in blobs, which needs to be decoded.
This is handled by a fission function, which receives a POST request containing the repository commit, and returns a JSON of the data we want. \newline

The container begins by opening a websocket to the firehose endpoint \texttt{com.atproto.sync.subscribeRepos}.
When we receive a \texttt{RepoCommit}, we call the fission function \texttt{processPost} which handles decoding the message.
A commit could be a like on a post, a new post, a repost, or some other type of update.
\texttt{processPost} returns either a 200 with a JSON of the post which contains the \texttt{cid}, \texttt{did}, \texttt{text} and \texttt{createdAt} fields, or a 204 if the commit was not a post.
Bluesky uses \texttt{cid} as the unique identifier of a commit, so we reuse this in the elastic search index as the id.
\texttt{did} is an identifier for the author of the post, \texttt{text} is the body of the post and \texttt{createdAt} is the post timestamp. 
The response is indexed to ElasticSearch using the bulk indexer. \newline

We index the documents to Elastic Search from the firehose container.
We could have the fission function index documents to ElasticSearch, but then each function would need to obtain a client login for each document, and index each document individually.
In this case, the volumne of requests results in the ElasticSearch instance becoming overwhelmed by the number of requests, and would stop accepting requests.
Using the bulk indexer for ElasticSearch reduces indexing overhead and increases speed.

\subsection*{Open Australia}

The OpenAustralia (OA) API provide a REST API which we can use to query the Australian parliament's Hansard reports, and also some user comments.

A redis message queue is used to handle the data inflow from the OpenAustralia API.\
The queues are created by sending a post request to the \texttt{enqueue} function, which allows messages to be stored in ``topic'' queues. 

The general structure of the OpenAustralia data inflow is as follows:
\begin{enumerate}
    \item An ``entrypoint`` fission function is called, which adds the parameters (keys) of the request to the redis message ueue \texttt{oa\_debate\_keys}.
    
    \begin{lstlisting}[frame=none,numbers=none]
    { 
        "house": "senate" or "representatives",
        "date" or "person": date in format YYYY-MM-DD or person id
    }
    \end{lstlisting}


    \item The \texttt{oa-debate-harvester-by-details} fission function is triggered by this redis message queue. 
    
    This function queries the OpenAustralia API for the debate details, and then queries the OpenAustralia API for the comments on that debate. This raw data is then stored in another redis queue \texttt{oa\_debates\_data}.
    \item The \texttt{debate-adder} fission function is triggered by this redis message queue. 
    The data is then processed and stored in the elastic search index \texttt{oa-debates-comments}.
    
    A parent-child relationship is created between the debate and the comments, so that we can query the comments for a specific debate.
    And also between debates and their parent topics, so that we can query the debates for a specific topic.
\end{enumerate}

There are three main entrypoints to gathering OpenAustralia data.

\begin{itemize}
    \item \texttt{oa-date-lister} - Queries the OpenAustralia API for dates in a year where debates were held.

    This uses a httptrigger route \texttt{oa-dates-with-debates}, providing the year as a parameter.

    \item \texttt{oa-person-lister} - Queries the OpenAustralia API for all debates held by a specific person.
    
    This uses a httptrigger route \texttt{oa-people-year-house}, providing the year and house (senate or represenatives) as parameters.

    \item \texttt{oa-daily-debate-harvester} - Queries the OpenAustralia API for all debates held on a specific date, and then queues the results for processing.
    
    This is accessed through a time trigger, which runs once a day.
\end{itemize}

The first two entrypoints are called manually to scrape past data from the backlog, while the third is called daily to gather new data.
All of the functions needed to gather and process OpenAustralia data are contained within the \texttt{oa-debates} fission package.

\subsection*{Reddit Data Processing Architecture} 

Data harvesting and processing architecture for Reddit is designed to handle both ad-hoc, parameterized scraping of Reddit data and a regular daily collection of recent posts. The system uses a Fission serverless workflow and Redis message queues to orchestrate smooth data flow from the Reddit API to Elasticsearch.

The Reddit data pipeline, among other things, consists of the following major Fission functions and components:

\begin{enumerate}
    \item \textbf{Entrypoint Functions (Initiating Data Collection):} 
    There are two main Fission functions that act as entrypoints for Reddit data collection:

    \begin{itemize}
        \item \textbf{HTTP Triggered Harvester (\texttt{reddit-harvester-new-r}):}
            This function gets started via an HTTP POST request through the route  \texttt{/reddit-new/scrape/}.
            It is defined by the \newline \texttt{reddit\_harvester.py:main} entrypoint.
            The function expects a JSON payload specifying:
            \begin{itemize}
                \item \texttt{keywords}: A list of keywords to search for.
                \item \texttt{subreddits}: A list of subreddit names.
                \item \texttt{limit} (optional, default: 10): The maximum number of posts to retrieve per keyword/subreddit.
                \item \texttt{scrape\_from} (optional, default: "2000-01-01"): A start date (YYYY-MM-DD format) for fetching posts created after this date.
                \item \texttt{scrape\_until} (optional, default: current date): An end date (YYYY-MM-DD format) for fetching posts created up to this date.
                \item \texttt{sort} (optional): The sorting method for Reddit search results (e.g., "new", "top", "relevance").
            \end{itemize}
            The \texttt{reddit\_harvester.py} module uses the PRAW library to query the Reddit API and search for posts matching the supplied criteria. It also extracts top-level comments for each of the retrieved posts using a helper function named  \texttt{get\_post\_comments}
            The posts and their corresponding comments are then packaged as JSON objects. These JSON objects, representing a list of posts and comments, are added to a Redis message queue called \texttt{reddit\_posts\_queue} using the utility function \texttt{util.py:enqueue\_data}, which performs the enqueue operation via an HTTP POST request to a generic Fission \texttt{enqueue} function.

        \item \textbf{Timer Triggered Daily Harvester (\texttt{reddit-daily-job}):}
            This function is automatically executed once a day, triggered by a Fission timer configured with a \texttt{@daily} cron schedule.
            It initiates the \texttt{reddit\_daily\_harvester.py:main} entrypoint.
            The script searches for posts through the Reddit API by referencing a hardcoded list of subreddits (\texttt{DAILY\_SUBREDDITS}) and keywords (\texttt{DAILY\_KEYWORDS}) within the script.   
            It focuses on posts created within a specific recent period defined by \texttt{DAILY\_SCRAPE\_DAYS\_AGO} (e.g., 1 day by default, but configurable as per the script's constants). The constants \texttt{DAILY\_LIMIT\_PER\_KEYWORD\_SUBREDDIT} and \texttt{DAILY\_SORT\_METHOD} are also mutable.
            It operates in a manner similar to the HTTP harvester, retrieving posts (and possibly their top-level comments via \texttt{get\_post\_comments\_for\_daily}) (depending on some configuration there). The JSON-formatted data is then sent to the \textbf{\texttt{reddit\_posts\_queue}} using \texttt{util.py:enqueue\_data}.
            Hardcoding these subreddits and keywords in the daily harvester is deliberate, to facilitate daily scraping in an automated fashion; very well might be put into the \texttt{shared-data} ConfigMap for more flexibility. 
    \end{itemize}

    \item \textbf{Data Queuing (Redis):} 
    Both harvesting functions output their collected and formatted data (typically JSON strings representing batches of posts/comments) to a central Redis list acting as a message queue named \textbf{\texttt{reddit\_posts\_queue}}.  
    Address for the Redis instance used by the Fission Message Queue Triggers is: \texttt{redis-headless.redis.svc.cluster.local:6379}.
    

    \item \textbf{Data Indexing into Elasticsearch (Adder Function):}
    The \textbf{\texttt{reddit-to-es-new-r}} Fission function, with its entrypoint at \texttt{reddit\_to\_elasticsearch.py:main}, is responsible for consuming data from the \texttt{reddit\_posts\_queue}.
    There is a Fission Message Queue Trigger (MQT) of \texttt{mqtkind keda} watching \texttt{reddit\_posts\_queue}.
    When new messages (batches of posts/comments) get enqueued, KEDA starts up \texttt{reddit-to-es-new-r}.
    The function loops over posts or comments in the passed batch.
    For each item, it checks for any document with the same \texttt{post\_id} (acting as the unique identifier for both posts and comments) already present within the \textbf{\texttt{reddit}} index in Elasticsearch, to avoid duplicates. 
    If the item does not exist within this index, it gets indexed within the \texttt{reddit} index. The mapping for the index is contained within \texttt{reddit-index.json}.
    If processing of the message fails after the configured number of retries (say 3 retries), the troublesome message is put onto an error topic/list in Redis named \texttt{reddit\_adder\_error} for further analysis and debugging.
\end{enumerate}

This multi-tier, queue-based architecture allows maximized decoupling of data harvesting and data indexing, with resilience and scalability features. All Fission functions for the Reddit pipeline reside within the \texttt{reddit-harvester-new-r} Fission package and use the \texttt{shared-data} ConfigMap for their configurations (such as API keys, Elasticsearch connection details, and so on).

\subsection*{Sentiment Analysis}

\paragraph*{Choosing an Analysis Method} 
In order for us to perform sentiment analysis on the posts from the various social media platforms we whose, we had to choose between different avaiable options. However, since we didn't want to reinvent the wheel and risk having analysis of lesser quality we decided to use a pre-existing library. Now the choice of library came down to ease of use and overall consistency with our project.

This is why we decided to use the \texttt{VADER} library. VADER is a lexicon and rule-based sentiment analysis tool that is specifically tuned to sentiment analysis in social media posts and text. It is capable of detecting sentiment in text, and it is particularly effective for short texts such as tweets or posts.

\paragraph*{Putting it into Practice}
For our project, we decided to keep the sentiment analysis simple and only use a couple of values from the vader sentiment analysis function which are the following: positive, negative and neutral.

The next step was of course to implement the analysis function in our code and make it available for other functions to use. This was done by creating a new fission function called \texttt{sentiment-analysis} which takes in a string and returns the sentiment analysis values. We had to manage the dependencies and make sure that the fission function had access to the \texttt{VADER} library. This was done by creating a fission package.

\paragraph*{Example}
\begin{quote}
    \textit{``The Liberal Party is not a party that is both good and bad !''}
\end{quote}

The sentiment analysis function would return the following values:
\begin{quote}
    \texttt{'neg': 0.279, 'neu': 0.462, 'pos': 0.259, 'compound': -0.1154}
\end{quote}

\subsection*{Named Entity Analysis}
\paragraph*{Why do we need Named Entity Analysis?}
Named entity analysis is a crucial part of this project as it allows us to identify and extract relevant subjects, persons and many others from the different posts and debates we collected.

\paragraph*{Setting up the NER}
Now, setting up the NER was a bit more comlpicated as the library we chose to use \texttt{spacy} has dependencies and python version requirements and is also pretty heavy.
This meant that we had to create a docker image with all the dependencies pre-built in it and then use it as an environment for our fission function.

\paragraph*{Example}
\begin{quote}
    \texttt{"text": "Barack Obama was born in Hawaii and worked in Washington."}
\end{quote}
The NER function would return the following values:
\begin{quote}
    \texttt{{"PERSON": ["Barack Obama"],"GPE": ["Hawaii", "Washington"]}}
\end{quote}

\subsection*{Analysis}

Performing the analysis described above takes a non trivial amount of time. 
3 simple options come to mind to handle this.

\begin{itemize}
    \item Calculating the analysis results for all posts. 
        Given the large number of posts we have in some indexes, bluesky in particular, this would be infeasible.
    \item Run the analysis on the posts queried by the user. 
        This may take a long time if the query contains a large number of posts.
    \item Setup a fission function to periodically calculate and store the values for a subset of posts.
        This would be a better option than the two previous ones, but requires foresight into what posts are relevant, or increased effort in changing the focus.
\end{itemize}

Given the disadvantages of each, instead we implement a hybrid of 2 and 3.
We calculate the values for posts queried by the user, and cache these in a separate elastic search index. 
When the user queries those posts again, we pull values from the cache index instead of using the analysis functions.
This process is described in Algorithm~\ref{alg:cap}.
To increase throughput, we send the cache function a list of post id's to retrieve, reducing the overhead of authenticating elastic search clients.

\begin{algorithm}
    \caption{Caching Analysis Results}\label{alg:cap}
    \begin{algorithmic}
        \State $ \texttt{Ids} \gets [id_0, \dots, id_n] $ 
        \State $ \texttt{Index} \gets index $ \Comment{ElasticSearch index containing the id's}
        \State $ \texttt{Field} \gets field $ \Comment{Field which is being analysised}

        \State $ \texttt{CacheEntry} \gets \texttt{queryESForIds}(\texttt{Ids}, cacheName, \texttt{Index}) $ \Comment{\parbox[t]{.4\linewidth}{Fetch any entries from the elastic search cache index which match the index and any id in Ids}}

        \For{$ id $ in $ \texttt{Ids} $}
            \If{$id$ not in $ \texttt{CacheEntry} $}
                \State $ text \gets \texttt{queryES}(\texttt{Index}, id, \texttt{Field}) $ 
                    \Comment{\parbox[t]{.4\linewidth}{Query \texttt{Index} for the document with id 
                    \texttt{id} and retrieve the field \texttt{Field}}}
                \State $ value \gets \texttt{queryAnalysisFunction}(text) $ 
                    \Comment{Compute the value to cache}
                \State \Call{insertES}{cacheName, \texttt{Index}, id, value} 
                    \Comment{Insert the value to the cache index}
                \State \Call{append}{\texttt{CacheEntry}, \{id, value\}}
            \EndIf
        \EndFor

        \State \Return $ \texttt{CacheEntry} $
    \end{algorithmic}
\end{algorithm}

\subsection*{User Interface}

The user interface consists of Jupyter Notebook, and fission functions to collect the data.
We have 4 fission functions,

\begin{itemize}[label=-]
    \item \texttt{/ui/sentiment/keyword/:keyword/start/:start/end/:end} Aggregates the sentiment per day from the start date to the end date, filtering posts by a keyword.
    \item \texttt{/ui/sentiment-averager/type/:type} Calculates the average sentiment for posts containing a keyword.
    \item \texttt{/ui/counts/start/:start/keyword/:keyword} Calculates the number of posts containing a keyword, per day since the given start date.
    \item \texttt{/ui/named-entities/label/:label} Finds the named entities belonging to a given label.
\end{itemize}

Each function queries the relevant fission functions, or ElasticSearch, and collects the result into a form ready to be displayed to the user.
We use MatPlotlib for most of the visualisations, and the WordCloud python package for creating word clouds of named entities.

\subsection*{Fission}

\subsection*{Fission Specs}

In alignment with the principle of declarative application management, we used fission specs to define the desired state of our fission setup. 

This allows us to easily manage the state of our system, add new functions and edit the configuration of existing functions.
It also means that our infastructure is ``self healing'', as fission will automatically re-deploy any functions which are not in the desired state.
This was espeically important when working in a shared environment with a large number of group members.

\section*{NeCTAR Research Cloud}

\subsection*{Platform Discussion}

\paragraph*{Benefits} A benefit of the NeCTAR Research Cloud and fission is the ability to use multiple programming languages.
Due to issues with python, we switched the Bluesky harvester to Golang.
Additionally the analysis caching was written in Go, the main benefit of using Go is the easy concurrency for requesting new analysis results.
Team members not familiar with Go could still use the cache function since it could be accessed as a fission function from Python code.

\paragraph*{Challenges} A major challenge our group faced was adapting to the shared nature of the NeCTAR research cloud.
At several points during the project, we encountered issues arising from multiple people attempting to modify the system at once.

Some of the minor issues included conflicting fission deployments, or source code being removed without the fission yaml specs being removed, resulting in a spec that could not be deployed.
However, we also encountered more serious issues.
Near the beginning of the project, the cluster was deleted and the setup was unexpectedly re-run, meaning that we had to re-build our software stack.
And not long after the cluster was re-setup, a duplicate ElasticSearch deployment was made to the default namespace, alongside the ElasticSearch deployment in the elastic namespace.
This didn't seem to break anything and was removed without affecting the project.
A challenge of NeCTAR is the lack of Role Based Access Control within a group, meaning we couldn't have one person responsible for managing the cluster.

Even at a small group scale, it was difficult to coordinate the changes being made to the system.
This was mitigated by the use of fission specs, as we could easily re-deploy the functions to a different worker node.
These challenges highlighted the benefits of using an Infrastructure as Code approach, as we could recover from these setbacks.

Another challenge of using fission is the inability to carefully instrument programs when issues occur within a function.
In this case, Bluesky would not work in python.
A simple function to pull from a feed would work locally but not in fission.
Tracing it back to a minimal example, having \texttt{import atproto} in a python file would cause the function to not execute at all.
So we switched to using Go for the Bluesky handling.

\subsection*{Application Scaling}

\paragraph*{Bluesky} To handle processing the large number of posts in the bluesky firehose, we utilise a new deploy pool of fission functions.
We set the minimum number of pods to 1, and the maximum to 30. 
This allows fission to increase or decrease the number of pods, up to 30, depending on the current load.
Since the firehose container is implemented using Go, we spawn a Go thread for each request, allowing the firehose to scale vertically to meet demand since we can't scale horizontally. \newline

Using grafana, we can monitor the system resources used by the bluesky firehose.
At the peak, the fission function handles 35,000 requests per second, with around 5,000 of those requests containing a post, shown in Figure~\ref{fig:bluesky-post}.
The firehose websocket pod utilises 0.8 cpu's at the peak, and 0.4 cpu's for the fission function, shown in Figure~\ref{fig:bluesky-cpu}. \newline

\begin{figure}[h]
    \centering
    \includegraphics[scale=0.3]{report_images/bluesky-posts.png}
    \caption{Bluesky fission function calls.}
    \label{fig:bluesky-post}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[scale=0.3]{report_images/bluesky-cpu.png}
    \caption{Bluesky cpu usage.}
    \label{fig:bluesky-cpu}
\end{figure}

\paragraph*{OpenAustralia} 
The data influx of the OpenAustralia api was of a less intense ``velocity'' compared to that of BlueSky and Reddit.
This meant that the regular scraping could be done on a daily basis, gathering data from debates held two days prior, with to allow complete collection of data, as the Hansard reports are finalised by 5pm the day after the debate.
This was achieved using a time based fission trigger \texttt{oa-daily-debate-harvester}, which runs once a day.
Similarly to the BlueSky setup, the number of pods were dynamically allocated as needed. However, due to the lower amount of incoming data the minimum number of pods was set to 0, as the resources required when starting a new pod would not significantly affect the performance.
\\
The message queues used a Keda workload autoscaler.
This monitored the number of messages in the queue, and scaled the number of pods up or down depending on the number of pods required.
This helped handle the variabible load of the OpenAustralia API, as the number of messages in the queue could vary from 0 to 1000+, especially when gathering data from previous years. 

As a side comment, the usefulness of the \texttt{oa-daily-debate-harvester} was limited while collecting data for our project, as The Houses of Parliament are in recess from 16 May until 25 May 2025 and so no new debates were available to add.

\paragraph*{Analysis} In the System Architecture section, we discussed the cache used for analysis functions.
When we don't have a cached value for a post/debate, we need to query the analysis functions to compute the values.
If there is a large number of posts which are not cached, this can take a long time.
To speed up calculations, we take advantage of fission and Go.
If we find a value we need to calculate in the caching function, we spawn a go thread.
This thread is responsible for calling the fission function for calculating the value and caching the result.
This allows the cache function to calculate all missing values concurrently, while using functions from other languages such as Python.
The ability of fission to scale automatically allows the large number of function calls generated by this process to be handled.
Additionally the cache function requires less vertical scaling compared to calculating the values in the cache function, since most of the threads will be inactive while waiting for the response from the fission function.

\subsection*{Error Handling}

\paragraph*{Bluesky} The Bluesky firehose client is deployed as a container, instead of a fission function, since the websocket can listen indefinitely.
This container is deployed to kubernetes with one replica. 
This means the container will be re-deployed to another worker node if one fails, and kubernetes will spin up a replica if the current one crashes.
If an error occurs in the fission function which processes posts, a server error code and the error message is returned in the response.
The Bluesky client logs out the response and continues on if it receives an error from the fission function.

\paragraph*{OpenAustralia} 
A modular, ``Event Driven Architecture'' approach was taken to handle errors in the OpenAustralia API.\ 
The logic associated with querying the OpenAustralia API was split into three key parts: the entrypoint functions, the harvester, and the adder.

These are connected by redis message queues, each with their own ``error topics''.
This seperation of functionality improves the robustness of the system, as we can see exactly where any errors occurred, and retry only that part of the process. Somethiungsomething Event driven architecture
\
It also means that if one part of the process fails, the outputs of the other functions will still be saved, allowing the process to continue without data loss.

This was particularly useful when querying the OpenAustralia API, as the formatting of responses is often unpredictable, even within the same endpoints.
\\
The immediate httptrigger entrypoint functions do not have any retry logic, as they are only called once manually, and the data is stored in the redis queue.
The \texttt{oa-debate-harvester-by-details} and \texttt{debate-adder} functions are triggered by the redis queues, and have retry logic built in. 
Both of these functions will retry up to 3 times if they encounter an error, and will log the error message to their respective ``error topic'' redis queue.
\\
This strategy is certainly not perfect, as it does not handle all possible errors, but it does allow us to see where the errors are occurring, and retry the failed functions.

\paragraph*{Reddit}
Similarly to the OpenAustralia API, the reddit harvester is split into three parts: the entrypoint, the harvester and the adder. 
This provided all the same benefits as the OpenAustralia API, including seperation of functionality, and the ability to retry failed function and greater insight into where errors are occuring.

\subsection*{Integration Testing}

We have automated testing in the \texttt{src} directory. 
The script \texttt{run\_tests.sh} runs the each of the tests in one script.
For Bluesky we test the process post function correctly decodes a post, and return a 204 status code for a non-post.
For the caching function, we test that calculated values are inserted to the caching ElasticSearch index, and if we retrieve a cached value, the value is the one in ElasticSearch and is not recalculated.
For OpenAustralia, we mock the date lister and person lister functions, to ensure they pull the correct data from the API and make the correct calls to the enqueue function.
For Sentiment and Named Entity analysis, we check the output of the fission functions match the expected output.

\section*{Analysis findings}

For Reddit, we collected data from a collection of subreddits related to Australia.
For Bluesky, we filter posts to those containing 'auspol', a common hashtag on Bluesky for discussing Australian Politics.

\subsubsection*{Topics Across Different Platforms}

To compare the topics of the posts and debates, we use word clouds to show the most common named entities in the posts and debates.
The python library \texttt{spacy} implements Named Entity Recognition, which we use to extract entities from text.
Word clouds are generated using the \texttt{wordcloud} python library.
Figure~\ref{fig:word-org} shows a wordcloud of organisations discussed in posts and debates across each platform.
Figure~\ref{fig:word-person} shows a wordcloud of people discussed in posts and debates across each platform.
Wordclouds are excel at displaying a large amount of data, and giving qualitative comparisons between different data points, but doesn't show quantitative differences in values very well. 
Figure~\ref{fig:freq-org} shows the top 10 organisations across the platforms in a bar chart.

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.4]{report_images/org_wordcloud.png}
    \caption{Word cloud of the most common ``Organisation'' entities in the posts and debates.}
    \label{fig:word-org}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.4]{report_images/person_wordcloud.png}
    \caption{Word cloud of the most common ``Person'' entities in the posts and debates.}
    \label{fig:word-person}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.4]{report_images/org_freq.png}
    \caption{Top 10 most common ``Organisation'' entities in the posts and debates.}
    \label{fig:freq-org}
\end{figure}

\subsubsection*{Sentiment Over Time Across Platforms} 

The overall sentiment of the posts and debates was calculated using the \texttt{VADER} library.
The sentiment was calculated for each post and debate, and then averaged over time.
This allowed us to compare the overall averaged sentiment of the posts and debates over time.
We found that this was relatively consistent across the platforms, with only small variations sentiment.
Figure~\ref{fig:bluesky-reddit-sentiment} shows the sentiment of Reddit and Bluesky from the 1st to the 19th of May 2025. 
Bluesky posts averaged a negative sentiment of around 7\%, while the average negative sentiment of reddit posts ranged from 5\% to 9\%. 
Both BlueSky and Reddit posts were generally mostly neutral, but slightly more positive than negative.

Figure~\ref{fig:open-aus-sentiment} shows the sentiment across two weeks of debates in 2021.
The sentiment is more positive than negative, with around 10\% positive and 5\% negative.
As more data is collected over time, we would be able to display sentiment across all 3 platforms for the same time period.

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.5]{report_images/bluesky_reddit_sentiment.png}
    \caption{Sentiment of posts related to Australian politics between 1st May 2025 and 19th May 2025}
    \label{fig:bluesky-reddit-sentiment}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.5]{report_images/openaus_reddit_sentiment.png}
    \caption{Sentiment of debates between 22nd August 2021 and 3rd September 2021.}
    \label{fig:open-aus-sentiment}
\end{figure}

\subsubsection*{Sentiment By Keyword Across Platforms}

To show overall sentiment around topics or keywords, we average the sentiment of all posts containing a keyword.
For OpenAustralia, if the topic was a person or party, the average sentiment of debates held by that entity were calculated.
If the topic not a person or party, debates mentioning that topic were averaged.

Figure~\ref{fig:pauline-sent-avg} shows the sentiment of posts discussing ``Pauline Hanson'' compared to the sentiment of debates where ``Pauline Hanson'' was a speaker. 
Figure~\ref{fig:albo-sent-avg} shows the sentiment of posts discussing ``Anthony Albanese'' compared to the sentiment of debates where ``Anthony Albanese'' was a speaker. 

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.7]{report_images/pauline_sent_avg.png}
    \caption{Sentiment of posts discussing ``Pauline Hanson'' and debates where ``Pauline Hanson'' was a speaker.}
    \label{fig:pauline-sent-avg}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.7]{report_images/albo_sent_avg.png}
    \caption{Sentiment of posts discussing ``Anthony Albanese'' and debates where ``Anthony Albanese'' was a speaker.}
    \label{fig:albo-sent-avg}
\end{figure}

\subsubsection*{Post Count By Keyword Across Platforms}

\begin{figure}[t]
    \centering
    \includegraphics[scale=0.4]{report_images/housing_count.png}
    \caption{Frequency of posts containing the word 'housing' per day.}
    \label{fig:housing-count}
\end{figure}

We wanted to investigate if debates influenced online discussions.
To do this, we calculated the number of posts and debates containing a set of keywords over time.
For example, Figure~\ref{fig:housing-count} shows number of posts and debates containing the keyword 'housing' over time.
Unfortunately there is limited overlap of Bluesky and Open Australia data due to the short timeframe of data collection.
In the future we may see a shift in the topics of online discussions, or the amount of discussion on a topic, after a debate is held.


\end{document}
